{
  "name": "@awesome-elements/site",
  "private": true,
  "version": "0.0.1",
  "description": "Awesome-Elements Website",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "start.no-open": "stencil build --dev --watch --serve --no-open",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^2.7.0",
    "@stencil/router": "^1.0.1",
    "@types/jest": "^26.0.24",
    "@types/marked": "^4.0.1",
    "jest": "^26.6.3",
    "jest-cli": "^26.6.3",
    "puppeteer": "^10.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "@awesome-elements/layout": "^0.1.15",
    "@awesome-elements/markdown": "^0.1.1"
  }
}
